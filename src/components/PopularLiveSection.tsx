import { Zap } from "lucide-react";
interface Creator {
  id: string;
  avatar: string;
  name: string;
}
interface PopularLiveSectionProps {
  creators: Creator[];
}
const PopularLiveSection = ({
  creators
}: PopularLiveSectionProps) => {
  return;
};
export default PopularLiveSection;